<Window x:Class="FitApp.View.WorkoutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitApp.View"
        mc:Ignorable="d"
        Title="WorkoutsWindow" Height="480" Width="320">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="btnUser" Content="" Width="50" Height="30" Margin="5,5,40,0"
  Background="#DC143C" Foreground="AliceBlue" VerticalAlignment="Top"
  BorderBrush="Red" BorderThickness="2" Padding="10" Command="{Binding WorkoutsWindow}" RenderTransformOrigin="0.38,0.748">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" 
              BorderBrush="{TemplateBinding BorderBrush}" 
              BorderThickness="{TemplateBinding BorderThickness}" 
              CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Label Content="FitTrack" FontSize="20" FontWeight="Bold" Width="92" Margin="15,0,15,0"/>
            <Button x:Name="btnAddWorkout" Content="+ Add Workout" Width="100" Height="30" Grid.Row="2" HorizontalAlignment="Center"
Background="#DC143C" Foreground="AliceBlue" VerticalAlignment="Center" Margin="0,0,0,10"
BorderBrush="Red" BorderThickness="2" Padding="10" Command="{Binding WorkoutsWindow}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
            BorderBrush="{TemplateBinding BorderBrush}" 
            BorderThickness="{TemplateBinding BorderThickness}" 
            CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
        <Label Content="Välkommen" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="40" FontFamily="Arial"/>



    </Grid>
</Window>
