<Window x:Class="FitApp.View.WorkoutsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FitApp.View"
        mc:Ignorable="d"
        Title="WorkoutsWindow" Height="480" Width="320">
    <Grid>
        <!--Rader-->
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Width="320">

            <!--Knapp som öppnar UserDetailsWindow-->
            <Button x:Name="btnUser" Content="{Binding Username}" Width="100" Height="30" Margin="5"
                Background="DarkOrange" Foreground="AliceBlue" BorderBrush="Crimson" BorderThickness="2" 
                Padding="10" Command="{Binding WorkoutsWindow}">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!--Label FitTrack-->
            <Label Content="FitTrack" FontSize="20" FontWeight="Bold" Margin="15,7,8,10"/>

            <!--Knapp som öppnar AddWorkoutsWindow-->
            <Button x:Name="btnAddWorkout" Content="+ Add Workout" Width="100" Height="30" Background="DarkOrange" 
                Foreground="AliceBlue" VerticalAlignment="Center" BorderBrush="Crimson" BorderThickness="2" 
                Padding="10" Command="{Binding WorkoutsWindow}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!--ListBox med Träningspass-->
        </StackPanel>
        <ListBox ItemsSource="{Binding Workoutlist}"  SelectedItem="{Binding SelectedWorkout}" Height="300" Width="300"
                 Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,40,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Date}" />
                        <TextBlock Text="{Binding Type}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--Knappar för att se detaljer, ta bort träningspass eller logga ut-->
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="50" Width="320">
            <Button Content="Details" Command="{Binding OpenDetailsCommand}" Width="80" Height="30" 
                Foreground="AliceBlue" VerticalAlignment="Center" BorderBrush="Crimson" BorderThickness="2" Padding="10" Margin="20,0,20,0">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="White" Offset="1"/>
                        <GradientStop Color="DarkOrange" Offset="0.7"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Remove" Command="{Binding RemoveWorkoutCommand}" Width="80" Height="30" 
                Foreground="AliceBlue" VerticalAlignment="Center" BorderBrush="Crimson" BorderThickness="2" 
                Padding="10" Margin="0,0,20,0">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="White" Offset="1"/>
                        <GradientStop Color="DarkOrange" Offset="0.7"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <Button Content="Sign Out" Command="{Binding SignOutCommand}" Width="80" Height="30" 
                Foreground="AliceBlue" VerticalAlignment="Center" BorderBrush="Red" BorderThickness="2" 
                Padding="10" Margin="0,0,20,5">
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="White" Offset="1"/>
                        <GradientStop Color="DarkOrange" Offset="0.381"/>
                        <GradientStop Color="DarkOrange"/>
                        <GradientStop Color="DarkOrange" Offset="0.7"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

    </Grid>
</Window>
